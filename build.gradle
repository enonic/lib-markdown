plugins {
    id 'java'
    id 'jacoco'
    id 'maven-publish'
    id 'com.enonic.defaults' version '2.0.1'
    id 'com.enonic.xp.base' version '2.0.0'
}

repositories {
    jcenter()
    xp.enonicRepo()
}

group = 'com.enonic.lib'
version = '1.0.0-B1'

jar {
    baseName = 'lib-markdown'
}

dependencies {
    compile "com.enonic.xp:script-api:${xpVersion}"
    compile 'com.atlassian.commonmark:commonmark:0.7.1'
    testCompile "com.enonic.xp:testing:${xpVersion}"
    testCompile "com.enonic.lib:lib-thymeleaf:2.0.0-B2"
}

jacocoTestReport {
    reports {
        xml.enabled = true
        html.enabled = true
    }
}

check.dependsOn jacocoTestReport

